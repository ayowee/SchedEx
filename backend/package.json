{
  "name": "schedex-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm run cleanup && nodemon server.js",
    "cleanup": "node -e \"try { require('child_process').execSync('npx kill-port 5000') } catch(e) { }\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^2.0.22"
  }
}
